program example(input, output);
var x, y: integer;

function funname(a, b: integer): integer;
var lo: integer;

begin
funname := a;
b := a;
lo := a
end;

begin
read(x, y);
y := funname(x, 12);
write(y) 
end.

